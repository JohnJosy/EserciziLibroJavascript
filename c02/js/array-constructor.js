'use strict'

var colors = new Array( 'white',
                        'black',
                        'custom');

var elementColors = document.getElementById('colors')
elementColors.textContent = colors[1]